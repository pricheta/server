services:
  debt_accounter:
    image: prichetaboss/debt_accounter:latest
    container_name: debt_accounter
    restart: unless-stopped

    deploy:
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 128M
    env_file:
      - .env

    networks:
      - pricheta

networks:
  pricheta:
    driver: bridge
    external: true