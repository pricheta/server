services:
  debt_accounter:
    image: prichetaboss/debt_accounter:release-2026-02-24
    container_name: debt_accounter
    ports:
      - "8000:8000"
    restart: unless-stopped

    volumes:
      - ./alembic:/app/alembic

    networks:
      - pricheta_network
